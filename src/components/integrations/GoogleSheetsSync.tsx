import { Card, CardContent } from '@/components/ui/card';

export default function GoogleSheetsSync() {
  return (
    <Card>
      <CardContent className="p-6 text-center">
        <p className="text-gray-600">
          Google Sheets integration is currently unavailable.
        </p>
      </CardContent>
    </Card>
  );
}