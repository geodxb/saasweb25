import { Card, CardContent } from '@/components/ui/card';

export default function AirtableLeadSync() {
  return (
    <Card>
      <CardContent className="p-6 text-center">
        <p className="text-gray-600">
          Airtable integration is currently unavailable.
        </p>
      </CardContent>
    </Card>
  );
}